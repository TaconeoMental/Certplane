server:
  address: "0.0.0.0:8443"
  tls:
    cert: /etc/certplane/broker/server.crt
    key: /etc/certplane/broker/server.key
    client_ca: /etc/certplane/pki/internal-root.crt
    min_version: "1.2"

policy:
  path: /etc/certplane/policy.yaml
  watch: true

identity_ca:
  provider: step-ca
  step_ca:
    url: https://ca.int.whisper.cl:9000
    fingerprint: "1234567890abcdef"
    root_ca: /etc/certplane/pki/internal-root.crt
    timeout: 10s

public_ca:
  provider: letsencrypt
  letsencrypt:
    email: infra@whisper.cl
    directory: production
    challenge: dns-01
    dns:
      provider: cloudflare
      propagation_timeout: 120s
      polling_interval: 10s
      credentials:
        name: /etc/certplane/secrets/cloudflare-token
    data_dir: /var/lib/certplane/lego
    renew_before: 720h

secrets:
  provider: vault
  vault:
    address: https://vault.int.whisper.cl:8200
    token_file: /etc/certplane/secrets/vault-token

state:
  path: /var/lib/certplane/broker.db

logging:
  level: debug
  format: json
  file: /var/lib/certplane/logs.json
